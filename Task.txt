1. В серверной части изменить масассив имен так, чтобы он был многомерным:
на первом уровне - имена в качестве ключей,
а значения по этим именам - ассоциативные подмассивы с элементами age и salary.
По запросу автодополнения по-прежнему выдавать только имена из этого массива;
2. На клиентской части добавить обработчик клика по пункту в списке автодополнения,
чтобы отправлялся еще один асинхронный запрос, содержащий выбранное имя в качестве параметра;
3. В серверной части создать обработчик второго асинхронного запроса,
который будет принимать выбранное имя, а в ответ отправлять детальную информацию о человеке из подмассива;
4. На клиентской части добавить обработчик ответа от сервера,
который будет выводить детальную информацию о выбранном человеке на странице браузера.